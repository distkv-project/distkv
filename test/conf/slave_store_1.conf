store {
    # Here is the server port that Dst-Server listens on.
    listeningPort = 8092
    # The number of shards in this store.
    shardsNum = 8
    # Set up mode.
    mode = DISTRIBUTED
    # mode = STANDALONE
    # Dmeta-server list str
    metaServerAddresses = "127.0.0.1:8081,127.0.0.1:8082,127.0.0.1:8083"
}
